spring:
  application:
    name: FileManager
  servlet:
    multipart:
      max-file-size: 400GB
      max-request-size: 400GB
      resolve-lazily: true
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
mybatis-flex:
  type-handlers-package: cn.wzpmc.filemanager.mybatis